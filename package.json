{
  "name": "gatsby-on-heroku",
  "description": "Gatsby Heroku example",
  "version": "1.0.0",
  "author": "Mickey Reiss <mickeyreiss@gmail.com>",
  "dependencies": {
    "gatsby": "^1.9.102",
    "gatsby-link": "^1.6.24",
    "gatsby-plugin-react-helmet": "^1.0.8"
  },
  "engines": {
    "node": "^8.0.0",
    "yarn": "^1.2.1"
  },
  "keywords": [
    "gatsby"
  ],
  "license": "MIT",
  "main": "n/a",
  "scripts": {
    "build": "gatsby build",
    "heroku-postbuild": "gatsby build",
    "start": "gatsby serve",
    "develop": "gatsby develop",
    "format": "prettier --trailing-comma es5 --no-semi --single-quote --write \"src/**/*.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "prettier": "^1.8.1"
  }
}
